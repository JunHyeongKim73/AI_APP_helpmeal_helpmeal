import 'package:flutter/material.dart';

class NoticeCheckPage extends StatelessWidget {
  const NoticeCheckPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('notice Check')
    );
  }
}