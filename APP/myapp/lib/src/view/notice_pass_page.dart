import 'package:flutter/material.dart';

class NoticePassPage extends StatelessWidget {
  const NoticePassPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('notice pass'),
    );
  }
}