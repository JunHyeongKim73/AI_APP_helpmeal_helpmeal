import 'package:flutter/material.dart';

class ReviewAnalysisPage extends StatelessWidget {
  const ReviewAnalysisPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('review analysis'),
    );
  }
}